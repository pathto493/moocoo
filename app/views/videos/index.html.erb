<div class="container">
  <% @videos.each do |vid| %>
    <div class="card-video-index">
      <iframe width="100%", height="80%" src="<%= vid.video_url %>" ></iframe>
      <div class="card-video-index-infos">
        <div>
          <h2><%= link_to vid.title, vid %></h2>
          <p><%= vid.description %></p>
        </div>
        <h2 class="card-video-index-infos">Number of Views: </h2>
      </div>
    </div>
  <% end %>
</div>
