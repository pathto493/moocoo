<div class="container">
  <div class="row">
    <div class="col-12">
      <h2 class="mt-3">Search for All Products</h2>
      <div class="cards-product my-3">
        <% @products.each do |product| %>
        <%= render partial: "shared/productcard", locals: { product: product } %>
        <% end %>
      </div>
    </div>
  </div>
</div>
